class PagesController < ApplicationController
  def home
    # @current_level = CurrentLevel.find(params[:current_level])
    # service = CalculationService.new(@current_level)
    # @success = service.perform!
  end
end
